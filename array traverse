#include <stdio.h>
#include <stdint.h>
int main()
{
	uint8_t arr[5]= {5,6,7,8,9};
	int size= sizeof(arr) / sizeof(arr[0]);
    int target=0X67;
    printf("new array is ");
	for(int i=0; i<=size-1; i++) {
		arr[i]=arr[i]+ 5;
		
		printf(" %d",arr[i]);
		
		}
		
	}
